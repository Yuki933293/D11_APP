# Verification

## Summary
- 代码已按模块拆分并 gofmt。
- `GOCACHE=/tmp/go-build go test ./...` 通过（无测试用例）。
- 已新增音量调节模块文件并完成格式化与编译验证。
- 已接入语音音量指令解析（调到百分比/加大/小点）并通过编译验证。
- 已修正 aw_dev_0_rx_volume 反向映射（值越大音量越小）并通过编译验证。
- 已扩展音量口令与相对百分比增减（如“声音调大20”）并通过编译验证。
- 已支持中文数字解析（如“音量调到四十/声音调大二十”）并通过编译验证。
- 已补充“减小”类口令解析（如“音量减小20”）并通过编译验证。

## Notes
- 默认缓存目录权限不足时，可通过设置 `GOCACHE` 指向可写目录完成验证。
- Python 版本未在本机执行验证（需要板载 arecord/aplay 与硬件环境）。
