# 自检报告

## 覆盖范围
- 拆分 main.go 到多文件模块（常量/状态、唤醒、意图、音乐、TTS、LLM、ASR、音频管线、控制与工具）。
- 新增音量调节模块（amixer 封装）。
- 接入语音音量指令解析与 ASR 处理分支。

## 风险与注意事项
- 默认 Go 缓存目录权限不足时需要显式设置 `GOCACHE`，否则 `go test` 会失败。
- 模块拆分后跨文件依赖较多，建议在板端环境做运行验证。

## 结论
本次修改仅涉及文件拆分与格式化，不改变原有逻辑路径；仍建议用户侧完成板端运行验证。
